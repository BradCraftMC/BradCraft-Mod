package net.stayer.bradcraft.sound;

import net.minecraft.registry.Registry;
import net.minecraft.sound.SoundEvent;
import net.minecraft.sound.SoundEvents;
import net.minecraft.util.Identifier;
import net.stayer.bradcraft.BradCraft;

import static net.minecraft.registry.Registries.SOUND_EVENT;

/* Class for the Married Life disc (code currently doesn't work, commented for now)
public class ModSounds {
    public static SoundEvent MARRIED_LIFE = registerSoundEvent("married_life");

    private static SoundEvent registerSoundEvent(String name) {
        Identifier id = new Identifier(BradCraft.MOD_ID, name);
        return Registry.register(SOUND_EVENT, id, new SoundEvent(id));
    }
}

     */